services:
  fastttsr:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      MODEL_CACHE_DIR: /cache
    volumes:
      - model-cache:/cache

volumes:
  model-cache:
